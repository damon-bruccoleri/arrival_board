# Title font (Smythe)

The app uses **Smythe-Regular.ttf** for the "Arrival Board" header when present.

Place `Smythe-Regular.ttf` in this directory (`tools/fonts/`).

You can override the path with the `TITLE_FONT_PATH` environment variable (see arrival_board.env.example).
